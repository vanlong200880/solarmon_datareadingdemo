const XlsxPopulate = require('.');

XlsxPopulate.fromBlankAsync()
    .then(workbook => {
        // Modify the workbook.
        workbook.sheet("Sheet1").printOptions('headings', true).printGridLines(true);
        // Write to file.
        return workbook.toFileAsync("./out.xlsx");
    });
