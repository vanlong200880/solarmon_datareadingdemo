var mybatisMapper = require('../index');
mybatisMapper.createMapper([ './ovsiannikov.xml' ]);
var param = {
    userId : 16,
    userRole : 'partner',
    companyId : 10
}

console.log(mybatisMapper);
var query = mybatisMapper.getStatement('store', 'getList', param, {language: 'sql', indent: '  '});
console.log(query);